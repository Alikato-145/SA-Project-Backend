FROM oven/bun:1.3.11-alpine

WORKDIR /app

COPY package.json bun.lock ./
RUN bun install --frozen-lockfile

EXPOSE 3000

CMD ["sh", "-c", "bun install --frozen-lockfile && bunx drizzle-kit migrate && exec bun run dev:docker"]
